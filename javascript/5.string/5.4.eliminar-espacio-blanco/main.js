const cursoReact= '  son 14 alumnos  '
console.log(cursoReact);
console.log(cursoReact.length);

//Eliminar al inicio. Este método nos ayudará cuando estamos validando datos que el usuario nos pase por el formulario
console.log(cursoReact.trimStart());

//Eliminar al final
console.log(cursoReact.trimEnd());

//eliminar en ambos:
console.log(cursoReac. trimStart().trimEnd());

