const numeroUno=1;
const numeroDos="2";
console.log(parseInt(numeroDos))//usamos una función parseint
console.log(numeroUno.toString()); // este es un método en el que primero se pone el nombre de la variable y se llama al metodo con la sintaxis de punto para convertirlo en string